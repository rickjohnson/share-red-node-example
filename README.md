# SHARE-red Example Node

This repository containts an example node that can be forked and used to develop new SHARE-red nodes

Any place where `example-node` is written, including the filenames, replace it with the name of your node. The main logic for the node can be found in `example-node.js`.
